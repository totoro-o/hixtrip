<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hixtrip.sample.infra.InventoryMapper">
    <!--mapper xml-->

    <!-- 查询库存信息 -->
    <select id="findBySkuId" resultType="InventoryDO">
        SELECT *
        FROM inventory
        WHERE sku_id = #{skuId}
    </select>


    <update id="updateInventory">
        UPDATE inventory
        SET sellable_quantity=#{sellableQuantity},
            withholding_quantity = #{withholdingQuantity},
            occupied_quantity = #{occupiedQuantity}
        WHERE sku_id = #{skuId}
    </update>
</mapper>
